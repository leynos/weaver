rules:
  - id: v2.where.metavariable-pattern
    message: detect function with specific param pattern
    languages: [rust]
    severity: ERROR
    match:
      pattern: fn $FUNC($X)
      where:
        - metavariable:
            metavariable: $X
            pattern: "self"
