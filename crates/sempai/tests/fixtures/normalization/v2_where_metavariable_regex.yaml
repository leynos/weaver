rules:
  - id: v2.where.metavariable-regex
    message: detect function names matching regex
    languages: [rust]
    severity: ERROR
    match:
      pattern: fn $FUNC($X)
      where:
        - metavariable:
            metavariable: $FUNC
            regex: "^get_.*"
